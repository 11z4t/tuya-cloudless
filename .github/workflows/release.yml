name: Create GitHub Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to create release for (e.g. v0.1.0)'
        required: true

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Determine tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "tag=${{ github.event.inputs.tag }}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.tag.outputs.tag }}
          fetch-depth: 0

      - name: Get previous tag
        id: prev_tag
        run: |
          TAG="${{ steps.tag.outputs.tag }}"
          PREV=$(git tag --sort=-version:refname | grep '^v' | grep -v "^${TAG}$" | head -1)
          echo "tag=${PREV:-$(git rev-list --max-parents=0 HEAD)}" >> "$GITHUB_OUTPUT"

      - name: Check if release exists
        id: check
        run: |
          TAG="${{ steps.tag.outputs.tag }}"
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/tags/${TAG}")
          echo "exists=$([ "$STATUS" = "200" ] && echo true || echo false)" >> "$GITHUB_OUTPUT"

      - name: Bundle lib into integration
        run: |
          # Copy lib/tuya_cloudless into custom_components/tuya_cloudless/lib/
          # so it is importable at runtime without any sys.path manipulation
          mkdir -p custom_components/tuya_cloudless/lib
          cp -r lib/tuya_cloudless custom_components/tuya_cloudless/lib/tuya_cloudless

      - name: Build integration zip
        run: |
          cd custom_components
          zip -r "../tuya_cloudless.zip" tuya_cloudless/ --exclude "tuya_cloudless/__pycache__/*" --exclude "tuya_cloudless/lib/tuya_cloudless/__pycache__/*"
          echo "Built tuya_cloudless.zip ($(du -sh ../tuya_cloudless.zip | cut -f1))"

      - name: Generate release notes
        if: steps.check.outputs.exists == 'false'
        id: notes
        run: |
          TAG="${{ steps.tag.outputs.tag }}"
          PREV="${{ steps.prev_tag.outputs.tag }}"
          VERSION=$(python3 -c "import json; print(json.load(open('custom_components/tuya_cloudless/manifest.json'))['version'])")
          {
            echo "notes<<NOTES_EOF"
            echo "## Tuya Cloudless ${VERSION}"
            echo ""
            echo "### Changes"
            echo ""
            git log "${PREV}..${TAG}" --pretty=format:"- %s" --no-merges | grep -v "^- Merge" || echo "- Release ${TAG}"
            echo ""
            echo ""
            echo "### Installation"
            echo ""
            echo "Install via [HACS](https://hacs.xyz) → Custom repositories → \`https://github.com/11z4t/tuya-cloudless\`"
            echo ""
            echo "Full changelog: [CHANGELOG.md](https://github.com/11z4t/tuya-cloudless/blob/main/CHANGELOG.md)"
            echo "NOTES_EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        if: steps.check.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: ${{ steps.tag.outputs.tag }}
          body: ${{ steps.notes.outputs.notes }}
          files: tuya_cloudless.zip
          generate_release_notes: false
